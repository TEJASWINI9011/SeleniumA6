package base;

import org.openqa.selenium.WebDriver;

public class DWSBase {
	static WebDriver driver ;  //make Web driver global
	public static void preCondition(String browser) {
		
		if(browser.equalsIgnoreCase("chrome"));
		
		
	}

}
